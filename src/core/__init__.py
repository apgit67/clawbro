"""core package — lazy re-exports to avoid circular imports.

Import from submodules directly (core.context, core.router, core.claude_client)
rather than from this package to prevent circular import issues.
"""
